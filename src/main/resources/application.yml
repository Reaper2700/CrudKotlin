spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/apisolid
    username: docker
    password: mypass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true